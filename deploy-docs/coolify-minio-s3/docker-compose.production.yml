# Coolify → Docker Compose (empty project). MinIO for production.
#
# Inside the container MinIO always uses:
#   9000 = S3 API
#   9001 = web console
#
# On the host (production):
#   10000 → 9000 (S3 API — use http://SERVER_IP:10000 in AWS_ENDPOINT when stacks are separate)
#   10001 → 9001 (console in the browser)
#
services:
  minio-production:
    # Pinned release (Docker Hub). Bump intentionally for security/features.
    image: 'minio/minio:RELEASE.2025-09-07T16-13-09Z'
    command: 'server /data --console-address ":9001"'
    environment:
      MINIO_ROOT_USER: SERVICE_s3_ROOT_USER
      MINIO_ROOT_PASSWORD: SERVICE_s3_ROOT_PASSWORD
    volumes:
      - 'minio_production_data:/data'
    ports:
      - '10000:9000'
      - '10001:9001'
    # Official MinIO images ship `mc`; they do not include curl (ubi-micro). See minio docker-compose upstream.
    healthcheck:
      test: ['CMD', 'mc', 'ready', 'local']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
volumes:
  minio_production_data: null
