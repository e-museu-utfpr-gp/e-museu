<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <!-- RefreshDatabase runs migrate:fresh: override DB name so the dev schema (e.g. laravel) is never dropped. Create this database once (see docker/mysql/init). -->
        <env name="DB_DATABASE" value="emuseu_testing"/>
        <!--
            MySQL-only tests use #[Group('mysql')] and skip when the driver is not mysql (see
            RequiresMysqlDriverConnection). CI should run ./run test with Docker MySQL.
        -->
        <!-- MAIL_MAILER=array: no real SMTP/network in tests; OutgoingMailIsConfigured still passes for the array driver. -->
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- Anti-bot off in tests (ANTIBOT_DRIVER=null → AntiBotVerifier::isActive() false). -->
        <env name="ANTIBOT_DRIVER" value="null"/>
    </php>
</phpunit>
